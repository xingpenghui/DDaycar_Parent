server:
  port: 10112
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_daydaycar?characterEncoding=UTF8
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  application:
    name: CarProvider
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/